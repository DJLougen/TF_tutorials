#policy: dictates action taken by the agenty in a given environment 
  #s is the current observation of the environment
  #s' is the state at the next step
  #p is policy 
  #r is reward 
  #(Q(s,a)) is the action-value function 
    #expected total reward starting from current state, which outputs the Q value. MAXIMIZE THIS.
  
  

#Cartpole:
  #y discount factor: discounts future rewards relatibe to immediate ones.
    #Relates rewards to the time domain
  